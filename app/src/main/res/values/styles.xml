<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_primary</item>
        <item name="colorPrimaryDark">@color/material_primary</item>
        <item name="colorAccent">@color/material_primary</item>
        <item name="android:statusBarColor">@color/material_primary</item>
        <item name="android:navigationBarColor">@color/material_surface</item>
        <!-- 移除API 23+的特性，确保兼容性 -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.Material3.Dark">
        <item name="android:statusBarColor">@color/material_primary</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.Material3.Dark" />
</resources>